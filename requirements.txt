# EduVision — Dependencies
# Preferred installation: `uv sync` (uses pyproject.toml + uv.lock).
# Pip fallback: `python -m pip install -r requirements.txt`.

# Vision and machine learning
ultralytics>=8.4.0,<9
opencv-python>=4.8.0,<5
torch>=2.2.0
torchvision>=0.17.0

# Image and data processing
numpy>=1.26.0,<3
Pillow>=10.0.0,<13
PyYAML>=6.0,<7
scipy>=1.11.0,<2
tqdm>=4.65.0,<5
albumentations>=1.4.0,<3
matplotlib>=3.8.0,<4

# Dataset/model utilities
huggingface-hub>=0.20.0,<2

# Tests
pytest>=8.0.0,<9

# Optional face detection/recognition dependencies are in requirements-face.txt.
